---
version: 1
slug: "src-landing-tsx"
primary_target: "src/landing.tsx"
related_targets: ["src/style.css","src/brand.tsx","index.html"]
---

# Landing page

Mode: Persuade. Existing Weaver’s Draft visual identity is preserved: indigo, cotton, gold actions, coloured human threads, Jost typography.

Product behavior: current implementation and PRODUCT.md. PROJECT-NARRATIVE.zh-TW.md supplies the concise project story; implementation and documentation must agree before a capability appears as product copy. English copy; retain groupthink, critical thinking, GPT-Live-1, Codex, WebMCP, structured tools and Mermaid.

Visitor path: hero and create/join → lost ideas and independent thinking → Omni, Muse, Codex → agent-controlled browser and WebMCP → shared structured whiteboard → one contribution workflow → accurate privacy disclosures → create-room CTA.

Proof: labelled synthetic woven conversation and an interactive illustrative whiteboard showing agent-generated and human-refined states. No commercial claims or measured efficacy. The board demonstrates why the controlled meeting environment provides agents structured context and precise edits, with human visual editing on the same artifact.

Preserve functional camera/microphone preview, settings, room-code entry, invite prefill, loading/error states and create/join actions. Regular-browser use remains available. Preserve accurate WebRTC/TURN, AI-provider, Worker and local-browser-storage disclosures. Agent configuration and coordination are stored by the room authority; bounded transcript/reminder evidence passes through the Worker for private analysis. Browser text checkpoints include private Muse lines and expire after 12 hours. Do not claim all meeting data stays in-browser, the Worker never receives transcript text, or a reminder pause control exists.

Responsive: existing desktop two-column hero with room panel, single-column tablet/mobile and invite-first ordering. Agent roles use open rows, whiteboard uses a two-state accessible control and responsive SVG, no heavy editor bundle in the landing.

Omni prepares silently and requires approval for each public spoken question. Muse offers private text, dictation and Live voice; private voice pauses the room microphone and captions. Reading a completed Muse reply aloud shares only the selected text. Shared whiteboard edits are implemented with Excalidraw and supported Mermaid flowchart/graph imports. Room posting requires an enabled permission and an explicit request for built-in Muse. External Codex tool access depends on WebMCP support in its browser.
